query conveyorsOrder($orderId: String!) {
  conveyorsOrder(orderId: $orderId) {
    conveyor {
      _id
      logo {
        name
        urls {
          webp {
            small
          }
        }
      }
      name
    }
    value
    error
  }
}
